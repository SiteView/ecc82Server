#include "bstree.h"

using namespace svutil;

/*template<class T>
void sorttree<T>::InsertNode(T element)
{
	treenode<T> *pNode=CreateNode(element);
	if(pNode)
		InsertNode(pNode);
	else
		puts("CreateNode failed");

}*/